import State from './BaseState';

export default class TurningState extends State {
    constructor(machine) {
        super(machine);
        // is movement true
        // update player position

        // let _Q = new THREE.Quaternion();
        // let rotation = this.entity.quaternion.clone();
        // let _A = new THREE.Vector3();
        // if (intents.left) {
        //     _A.set(0, 1, 0);
        //     _Q.setFromAxisAngle(_A, 4.0 * Math.PI * s * walkAceleretion.y);
        //     rotation.multiply(_Q);
        // }

        // if (intents.right) {
        //     _A.set(0, 1, 0);
        //     _Q.setFromAxisAngle(_A, 4.0 * -Math.PI * s * walkAceleretion.y);
        //     rotation.multiply(_Q);
        // }

    }
}