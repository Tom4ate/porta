import State from './BaseState';

export default class IdleState extends State {
    constructor(machine) {
        super(machine);
        // is movement true
        // update player position
    }
}